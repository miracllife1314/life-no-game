# P0 安全鎖 — 基準線(Baseline)測試報告

- **測試環境**：本機 LOCAL → 測試庫 `lwynbnphzpmbcawqvycy`（正式站完全未動）
- **測試日期**：2026-06-17
- **測試前提**：依指示**未**改登入、**未**導入 JWT、**未**啟用 RLS。本報告記錄「動工前的現況」，作為之後安全強化的對照基準。
- **資料安全**：測試前已備份（`backups/2026-06-17_1149/`，876 筆）；攻擊腳本全程自我清理，測試後測試庫已驗證**完全還原**（`score_logs` 255 筆、目標學員 500 分/student，與測試前一致）。

---

## 一、功能面：全部正常 ✅（5/5）

| # | 項目 | 結果 | 驗證方式與證據 |
|---|---|---|---|
| 1 | 打卡 | ✅ 正常 | 以合法 `approved` 打卡寫入 → 成功落庫；前端 dev server 回應 HTTP 200，畫面可載入。 |
| 2 | 排行榜 | ✅ 正常 | 13 位學員皆有分數可排序；排行榜讀 `profiles` 依分數排序，資料完整。 |
| 3 | 見證牆 | ✅ 正常 | 23 篇 `approved + share_to_witness` 貼文可顯示；本批另已加「20 篇後載入更多」分頁。 |
| 4 | 寵物升級 | ✅ 正常 | `user_pets` 資料一致；樣本中存在已進化寵物（`level=5, current_stage_index=2, evolved_at` 有值），升級流程運作正常。 |
| 5 | **自動加分** | ✅ 正常 | **實測**：合法 `approved +100` → 分數 500→600、`score_logs` 自動 +1 筆(+100,「完成任務」)；刪除打卡 → 自動退分回 500。加分/退分 trigger 皆正確。 |

> 結論：**現有系統功能完好**，這是一個乾淨、可信賴的對照基準。之後做安全強化時，這 5 項必須維持全綠。

---

## 二、安全面：F12 攻擊測試（第 6 項）

### ⚠️ 現況：6 項攻擊「全部得逞、零防禦」（0/6 被擋）

以下用 **anon key**（等同任何人開瀏覽器 F12 主控台的權限）對真實學員（測試林建良）發動攻擊：

| # | 攻擊手法 | 現況結果 | 風險 |
|---|---|---|---|
| A | 把自己 `role` 改成 `admin` | ❌ **得逞** | 任何學員可自我提權成管理員 |
| B | 直接把 `score` 改成 99999 | ❌ **得逞** | 任意灌分 |
| C | 匿名刪除 `profiles` 資料 | ❌ **得逞** | 可刪除任何學員帳號 |
| D | 偽造 `approved` 打卡灌 9999 分 | ❌ **得逞**（500→10499） | 繞過審核灌分 |
| E | 直接寫入 `score_logs` | ❌ **得逞** | 偽造分數日誌 |
| F | 呼叫 `adjust_score` RPC 提權加分 | ❌ **得逞** | 直接呼叫管理級函式加分 |

### 這個結果是「預期內」的，不是壞消息

目前系統**刻意**對 anon 全開（[fix_submissions_rls.sql](fix_submissions_rls.sql) 明確記載：因未走 Auth、`auth.uid()` 永遠為 null，故開放匿名讀寫）。所以「攻擊全得逞」正是**漏洞存在的鐵證**，也正是[安全強化施工計畫書](安全強化-施工計畫書.md)階段 0~2 要修補的目標。

---

## 三、結論與下一步

1. **功能基準線：5/5 全綠** — 現有系統可正常運作，可放心作為改造對照。
2. **安全基準線：0/6 被擋** — 確認 6 項漏洞全部存在，量化了風險、佐證了施工計畫的必要性。
3. **這份報告是「改造前(Before)」快照**。等[施工計畫](安全強化-施工計畫書.md)做到**階段 2（啟用 RLS + 防改分 trigger）**後，重跑同一支腳本（`scratch/security_baseline_test.mjs`），預期此表會從「全得逞 ❌」翻成「全被擋 ✅」，且功能面維持 5/5 全綠——那就是驗收通過。

> 重跑指令：`node scratch/security_baseline_test.mjs`（自我清理、只動測試庫）

---

### 附：測試林建良 測試前後狀態（證明測試庫未被污染）

| 時點 | score | role | score_logs 總數 |
|---|---|---|---|
| 攻擊前 | 500 | student | 255 |
| 攻擊後（清理還原） | 500 | student | 255 |
