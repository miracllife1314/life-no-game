# 上正式區 部署計畫（Runbook）

> 目標：把測試站(staging / 測試庫)目前的成果，安全地全部上到正式站(main / 正式庫)。
> 原則：**分層、照順序、每步驗證、出事可回滾**。真學員正在用，寧可慢不可錯。

---

## 0. 這次要上的東西（三層）

| 層 | 內容 | 風險 | 回滾方式 |
|---|---|---|---|
| 1️⃣ 程式碼 | 正式站落後 **32 個 commit**（重構 + UI修 + 去重修 + session綁定強化 377a8e0） | 低 | Vercel 一鍵回滾到前一版 |
| 2️⃣ 資料庫結構 | 索引 + Storage 政策強化 + **RLS 鎖 ~25 張表** | **高**（RLS 鎖錯會讓學員讀不到資料） | 每段附 Rollback SQL |
| 3️⃣ 資料清理 | 正式庫重複任務（同 bug 造成） | 中（刪資料） | 靠步驟 2 前的備份 |

> 分工：**Claude 負責程式碼 push + 寫好所有 SQL；你負責 Supabase 備份、貼 SQL、Vercel 環境確認**。
> （Claude 手上沒有正式庫 service key，無法也不該直接碰正式庫——這是刻意的安全設計。）

---

## 1. 前置（已完成 ✅）
- [x] 整理未提交改動、build 綠
- [x] 本地 staging 32+ commit 已推上 `origin/staging`（Preview 測試站自動重建）

**你先做**：開 Preview 測試站（Vercel staging 預覽網址）點一輪——登入、打卡、看任務有沒有重複、後台開得起來。**這代表「正式站上線後會長的樣子」**。確認 OK 再往下。

---

## 🚦 閘門 A：Vercel 環境變數（上程式前必過）
到 Vercel → 專案 → Settings → Environment Variables，**確認 Production**：
- [ ] **沒有任何 `_LOCAL` 結尾的變數**（若有 = 正式站會連到測試庫，污染學員資料，必須刪掉）
- [ ] `NEXT_PUBLIC_SUPABASE_URL` = 正式庫（`epolsiukauqfwxmjojia...`）
- [ ] `SUPABASE_SERVICE_ROLE_KEY`、`NEXT_PUBLIC_SUPABASE_ANON_KEY` 為正式庫那組
- [ ] `NEXT_PUBLIC_USE_REAL_AUTH` = （沒設或非 false 都代表「開」）
- [ ] `NEXT_PUBLIC_REQUIRE_SESSION` = 維持與現在正式站相同設定

> ⚠️ 注意：這批程式含 `377a8e0`（session 須與帳號綁定相符才放行）。上線後**少數仍持舊登入狀態的學員可能被要求重新登入一次**（6/17 已大規模重登過，多數人有有效 session，影響小）。屬正常、非故障。

---

## 🚦 閘門 B：正式庫備份（動資料庫前必過）
Supabase → 正式庫專案 → Database → Backups → **手動建立一次備份**（或確認當日 PITR 有效）。
- [ ] 已確認有一份可還原的備份（記下時間點）

---

## 2. 上程式（Claude 執行）
> 你過了閘門 A 後跟我說「上程式」，我才做：
1. `git checkout main` → `git merge staging`（快轉合併，無衝突）
2. `git push origin main` → Vercel 自動部署正式站
3. 等 Vercel 綠燈
4. **煙霧測試（你我一起）**：正式網址用學員帳號登入 → 打卡 → 看任務不重複 → 管理員登入後台開得起來
- [ ] 程式上線且煙霧測試通過

> 此時：程式已新，但正式庫 RLS 仍是舊的(較鬆)——**完全能正常運作**（鬆的權限不會擋功能）。可以安心停在這裡觀察，再進第 3 步。

---

## 3. 正式庫 DB 變更（你在 Supabase SQL Editor 執行，依序）
> 選**低流量時段**（深夜/清晨）。每段跑完看驗證結果，貼給我確認再跑下一段。
> 做法：開檔→全選複製 SQL→貼到正式庫 SQL Editor→Run。檔頭那句「只在測試庫執行」是開發期的防呆，**現在正式上線就是要在正式庫跑**。

### 前置驗證：確認 `is_admin()` 存在（RLS 政策依賴它）
```sql
SELECT proname FROM pg_proc WHERE proname IN ('is_admin','is_captain_of');
```
- [ ] 有列出 `is_admin`（6/17 遷移應已建立）。**若沒有**，先停，跟我說，我給你建函式 SQL。

### 3-1 索引（純加速、最安全）
- 檔案：[docs/階段1-步驟1-索引.sql](階段1-步驟1-索引.sql)
- 正式庫建議把 `CREATE INDEX` 改成 `CREATE INDEX CONCURRENTLY`（不鎖表、可線上跑）——我可幫你產正式版。
- 驗證：檔內查詢應列出 16 條 `idx_*`。
- [ ] 索引完成

### 3-2 Storage 政策強化（甲案：owner/admin）
- 用**強化版**（甲案，與測試庫一致）：[docs/storage安全強化-計畫書.md](storage安全強化-計畫書.md) 內的 SQL 區塊
- 驗證：上傳一張打卡照仍成功、見證牆/寵物圖仍顯示。
- [ ] Storage 完成

### 3-3 RLS Tier 1（16 張後台表：公開讀 + 限 is_admin 寫）
- 檔案：[docs/階段2-Tier1-RLS.sql](階段2-Tier1-RLS.sql)
- 驗證：檔尾查詢 16 張表 `rls_on=true`；**前台學員仍看得到任務/成就/課程**。
- [ ] Tier1 完成

### 3-4 RLS Tier 2（user_pets / course_attendance）
- 檔案：[docs/階段2-Tier2-RLS.sql](階段2-Tier2-RLS.sql)
- 驗證：學員仍看得到自己的寵物、出席紀錄；打卡/簽到仍能寫。
- [ ] Tier2 完成

### 3-5 RLS Tier 3（missions：公開讀 / 登入新增 / 管理員改刪）
- 檔案：[docs/階段2-Tier3-missions-RLS.sql](階段2-Tier3-missions-RLS.sql)
- 驗證：**學員每日任務頁正常顯示任務**；管理員仍能新增/排程任務。
- [ ] Tier3 完成

> 每段若驗證異常（學員讀不到東西）→ 立刻跑該檔尾的 **Rollback SQL** 還原該段，停下找我。

---

## 4. 清正式庫重複任務（你在 SQL Editor 執行）
- 檔案：[docs/正式庫-清重複任務.sql](正式庫-清重複任務.sql)
- 先跑【步驟1 乾跑】看會刪幾筆 → 貼給我確認 → 再跑【步驟2 真刪】→【步驟3】確認剩餘重複=0
- 安全鎖：只刪「0 打卡」的重複，有打卡的一律保留。
- [ ] 重複任務清理完成

---

## 5. 全站煙霧測試（你我一起，正式網址）
- [ ] 學員：登入 / 每日打卡 / 任務不重複 / 排行榜 / 寵物
- [ ] 小隊長：隊長面板 / 補簽
- [ ] 管理員：審核 / 調分 / 新增任務 / 名冊
- [ ] F12 Console 無紅色錯誤

全部綠 → 上線完成，更新 CHANGELOG。

---

## 回滾速查
- **程式出包** → Vercel → Deployments → 前一版 → Promote to Production（秒回）
- **某段 RLS 出包** → 跑該 SQL 檔尾的 Rollback（DISABLE ROW LEVEL SECURITY + DROP POLICY）
- **資料被誤刪** → Supabase Backups → 還原到閘門 B 的備份點
