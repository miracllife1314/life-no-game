# 開發脈絡與變更紀錄

> 這份文件給「**任何要檢查/修改這個專案的人或 AI**」先讀。
> 很多 bug 誤判,都是因為不知道下面這些「脈絡與陷阱」。**檢查程式前請先讀「Part 1」。**
> 維護方式:每次重要改動,在「Part 2」最上面加一條(日期 + 改了什麼 + 為什麼)。

---

## 待辦 / 決策待定 (TODO)

> 做完一項就把它移到「Part 2 變更紀錄」並打勾說明。新增待辦也加在這裡。

> 目前沒有待辦項目。

- ℹ️ **(已決定不做)** 11 筆 base64 舊圖搬 Storage —— 只有 11 筆、舊資料、心得文字都還在、已從載入排除(不影響速度/分數)。投報率低,決定不處理;那幾筆圖維持不顯示。
- ℹ️ **(已決定不做)** 邀約類「可完成多次(99 次)」任務的近似內容防呆 —— 維持只擋「一字不差」,避免誤擋真正不

同的邀約;重複的由大隊長手動退回。
- ✅ **(已完成 2026-06-25)** 小隊長假隱藏按鈕 → 改為只給大隊長(見 Part 2)。
- ✅ **(已確認 2026-06-25)** `schema_fixes_21` review_type 對齊:正式庫掃描已全部一致,不需再執行。

---

## Part 1 — 檢查前必讀的重要脈絡 / 陷阱

### 🗄️ 資料庫:有兩個
- `_LOCAL` 變數 = **測試庫**(本機 dev、Vercel Preview/staging 用)。
- 無後綴變數 = **正式庫**(Vercel Production / `main`,學員正在使用)。
- `lib/supabase.ts`:只要有設 `_LOCAL` 就用測試庫,否則用正式庫。
- **正式庫絕不可設 `_LOCAL` 變數**。

### 👤 profile_id 的陷阱(G 上次誤判的點)
- 一個「人」跨多期會有多筆 profiles,各筆 `id` 不同、但用 `profile_id` 串起來。
- **`app/page.tsx` 約 224 行:`const profile_id = p.profile_id || p.id`** → 前端拿到的 profiles,`profile_id` **永遠不會是 null**(null 會被補成 `id`)。
- 所以「手動新增的帳號 profile_id 在 DB 是 null」**不代表前端會出錯**,別用「DB 是 null」推論前端會壞。

### 🔑 captain_id 與 director_id「比對的鍵不同」(很容易搞錯)
- **captain_id**(隊長):用 **`profile_id`** 比對。例:`profiles.find(p => p.profile_id === team.captain_id)`。隊長候選下拉 value 綁 `c.profile_id`(正確)。
- **director_id**(大隊長):用 **`profiles.id`** 比對。例:`profiles.find(p => p.id === director_id)`(CaptainDashboard、TeamsTab)。大隊長下拉 value 綁 `p.id`(2026-06 修正,見 Part 2)。
- **改下拉前先確認這個欄位是用 id 還是 profile_id 比對**,不要一律改成同一個。

### 🧮 計分的真相在哪
- **分數真相 = `profiles.score`**,由資料庫 trigger(`schema_fixes_11`)原子更新。
- `score_logs` **只是顯示歷史**,不參與計分。少載/延後載不影響分數正確性。
- 審核通過給分:trigger 依 `submissions.score_awarded` 加分;上見證牆額外 +200。退回則扣回。

### ⏱️ 大隊長(admin)載入:延後載入策略(services/queries.ts)
- `fetchFull`(admin)所有查詢是 **`Promise.all` 並行** → **登入時間 = 最慢那一張查詢**。砍掉非最慢的表不會更快。
- **`score_logs` 延後載入**:登入「不載」,大隊長切到 `history` / `captain` 分頁時才載一次(`app/page.tsx` 的 `loadScoreLogs`)。所以 admin 一登入 `scoreLogs` 是空的,是**正常的**,別當成 bug。
- `applyTables` 用 `if (scoreLogsList.length > 0)` 才覆蓋,避免空陣列把已載入的歷程蓋掉。
- 學員/隊長走 `fetchScoped`,自帶本期歷程,不受上面影響。

### 🖼️ 證明圖片(proof_image_url)
- 曾有 11 筆是 **base64 整張圖**塞在欄位裡(撐到 ~10MB,害登入超慢)。
- `lib/supabase.ts` `uploadProofImage`:上傳 Storage 失敗會重試,**一律不退回 base64**(回 null),DB 不會再被撐肥。
- `fetchFull` 撈圖時用 `.not('proof_image_url','like','data:%')` **排除 base64**,只載正常 URL 圖(很小)。那 11 筆舊 base64 圖暫時不顯示,待日後搬 Storage。

### 🔒 RLS 現況(影響你用 anon key 能不能讀/寫)
- `profiles` 已鎖(個資批①):anon 讀不到;公開查詢改用 **`v_public_profiles`** 視圖(無手機)。
- `submissions` / `teams` 仍 `using(true)`:anon 可讀。
- DDL/資料修正:**Claude 沒有正式庫寫入權限**,需使用者把 SQL 貼到 Supabase SQL Editor 執行(慣例見 `docs/schema_fixes_*.sql`)。

### 🧩 任務:範本 vs 任務本體(review_type 同步)
- `mission_templates`(後台「任務管理」顯示的)= 範本;`missions`(學員實際打卡)= 任務本體。
- 改範本的 `review_type` 後,既有 `missions` 會**自動同步**(`hooks/useAdminMisc.ts` `handleUpdateMissionTemplate`,2026-06 加)。歷史不一致資料用 `docs/schema_fixes_21` 對齊。
- `review_type`:`auto`(免審核,自動通過,`reviewed_by` 會是 null)/ `leader`(隊長可審)/ `admin`(只有大隊長可審)。

### ✍️ Supabase 寫入一律檢查 error(否則「假成功」)
- `insert/update/delete` 失敗時 Supabase 回 `{ error }` **而不是 throw** → 只 `try/catch` 接不到、`await` 也不會報。
- 沒檢查就會「假成功」:畫面樂觀更新了、DB 其實沒寫進去 → 重整後悄悄消失(審核/按讚/報名/指派都踩過)。
- 規則:**每個寫入都 `const { error } = await ...; if (error) { 提示 + 中止/還原 }`**。多步驟刪除尤其要逐步檢查、出錯即停(見 `handleDeleteBatch`)。

### 📊 PostgREST 預設 1000 筆上限(大表必踩)
- Supabase/PostgREST 查詢**沒設 `.limit()` 時,預設最多只回 1000 筆**,且**沒排序就是回「最舊的」**。
- `submissions` 在 2026-06-25 破 1000 筆 → 後台撈提交被截到最舊 1000、**最新待審全沒了**,造成「學員提交了、後台卻沒任務可審」。
- 規則:**會成長的大表(submissions/score_logs…)查詢一律 `.order('created_at',{ascending:false}).limit(大數字)`**;像「待審核」這種絕不能漏的,**另外用 `.eq('status','pending')` 單獨撈一次再合併**(見 `queries.ts` fetchFull/fetchScoped)。

### 🔗 巢狀關聯在 JS 端補(services/joinData.ts)
- PostgREST 不自動 embed,所以 `submissions` 的 `.mission` / `.profile` 是在 `computeJoinedData` 用 Map 補上的。
- 後台若有畫面顯示空白/未知,先確認該元件是「靠 join 的欄位」還是「自己用 id 反查 props」。ReviewsTab 已改成用 props 反查(較穩)。

---

## Part 2 — 近期變更紀錄(新到舊)

### 2026-07-21(解決瀏覽器舊快取問題：自動清理 LocalStorage 中的『每日五感恩』舊版攻略)
- **問題**: 部分瀏覽器因先前快取了舊版 `nlp_guide_list` 鏡像，導致畫面依然顯示舊版任務「每日五感恩」。
- **改動範圍**:
  - `lib/guideConfig.ts`:
    * 將快取鍵提升為 `nlp_guide_list_v2`。
    * 在 `readLocalMirror` 及 `loadGuidesFromDB` 加入快取清洗機制：若發現快取或 DB 紀錄含有舊任務「每日五感恩」或「邀約入門體驗課」，會自動清理並強制回傳最新的預設 7 項任務攻略指引。

### 2026-07-21(精確對齊後台預設任務範本 7 項之攻略指引與折抵天數)
- **問題**: 前一版攻略中更新的折抵任務仍有部分與管理員當前配置的 7 項預設任務範本有微小落差（例如：沒有「邀約 NLP 入門課」與「推薦初階」，實際新版為「每週小組復盤會議」、「分享的心得」、「傳愛成功」與「參加課後課」）。
- **改動範圍**:
  - `lib/guideConfig.ts`:
    * 重新編排 `DEFAULT_BEGINNER_GUIDE` 與 `DEFAULT_ADVANCED_GUIDE`。
    * 將每日定課修行要點更新為「解生命數字、跟朋友連結、跟小組長回報與覆盤」，點記加分區間改為 `+60 ~ +260 EXP/天`。
    * 將每週修行要點改為「分享的心得、每週小組復盤會議」（+300 ~ +800 EXP）。
    * 天數折抵任務（Offsets）精確對齊當前現行 7 項任務：
      1. `每週小組復盤會議` (+500 EXP) ➔ 折抵 1.5 天
      2. `分享的心得` (+300 EXP) ➔ 折抵 0.9 天
      3. `傳愛成功` (+2000 EXP) ➔ 折抵 5.6 天
      4. `參加課後課` (+1000 EXP) ➔ 折抵 2.8 天
      5. `入選見證牆` (+200 EXP) ➔ 折抵 0.6 天

### 2026-07-21(更新攻略秘笈預設任務與點數對齊實際資料庫)
- **問題**: 攻略（Guide）中的預設打卡指南（認真/積極 Bullets）與折抵任務（Offsets）其任務名稱與點數仍為舊版文字，不符合實際資料庫中已經建立的日常與每週任務（如 50~150 EXP/天，折抵任務為「邀約 NLP 入門課」、「推薦初階」、「完成次感元個案 3次」等）。
- **改動範圍**:
  - `lib/guideConfig.ts`:
    * 更新 `DEFAULT_BEGINNER_GUIDE` 與 `DEFAULT_ADVANCED_GUIDE` 的預設任務文字與點數，以對齊當前 Supabase 資料庫。
    * 修改折抵任務標題（如 `邀約 NLP 入門課`、`推薦初階`、`完成次感元個案 3次`），使其對齊資料庫任務標題，並經由 `formatBrandText` 動態轉譯。

### 2026-07-21(神獸初始型態「混沌的蛋」更名為「創造的蛋」)
- **問題**: 配合系統全新調性，將神獸系統的初始型態「混沌的蛋」、「混沌破殼」、「混沌之卵」、「混沌初開」更名為「創造的蛋」、「創造破殼」、「創造之卵」、「創造初開」。
- **改動範圍**:
  - `components/Tabs/DailyQuestsTab.tsx`:
    * 將神獸對白 bubble、預設對白 pools 中的「混沌初開」改為「創造初開」。
    * 使用 `.replace(/混沌/g, '創造')` 動態替換神獸階段名 `stageName` 與階段描述 `stageDesc`，確保即便資料庫中未做 Migration，前端也會將其呈現為「創造的蛋」。
    * 修改進化引導文字、按鈕文字和 Modal 提示文字，將「混沌破殼」與「混沌的蛋」字詞改為「創造的蛋」與「創造破殼」。
  - `components/Admin/tabs/PetsTab.tsx`:
    * 更新後台管理員神獸培育進度表中的契合流派下拉選單選項「混沌的蛋」為「創造的蛋」。
    * 更新學生培育狀態 evolutionStatusText「混沌的蛋」預設值為「創造的蛋」。

### 2026-07-21(後台神獸編輯上傳提示一鍵複製提示詞功能)
- **問題**: 管理員在後台「神獸配置」分頁上傳圖片時，需要頻繁複製建議提示詞，原先無法一鍵複製。
- **改動範圍**:
  - `components/Admin/tabs/PetsTab.tsx`:
    * 新增 `copiedTip` 狀態與 `handleCopyTip` 點擊處理器。
    * 將上傳建議文字改為可點擊複製的排版，點擊後會呼叫 `navigator.clipboard.writeText` 將建議內容複製至剪貼簿，並顯示 `✓ 已複製提示詞` 綠色回饋樣式，2 秒後回復。

### 2026-07-21(以終為始每日抽卡更名為 C 圈祝福抽卡與文案改版)
- **問題**: 「以終為始每日抽卡」原先有「引導他人說出這個詞彙以完成今日修行」的任務指令，不符合使用者將其定位為「抽卡有祝福、帶著覺察度過一天」的新目標。
- **改動範圍**:
  - `components/Tabs/DailyQuestsTab.tsx`:
    * 將「以終為始」標題與說明文字，更名為「C 圈祝福抽卡」與「每天可抽取一張『C 圈給你的祝福』卡片」。
    * 將卡片正面的「🎯 引導他人說出這個詞彙以完成今日修行」練習引導文字，改為更溫馨且具覺察性的「✨ 這是 C 圈給你的今日祝福，請帶著這份祝福與覺察度過今天」。
    * 同步更新點擊卡片的提示訊息、卡片背面文案、日期標題與底部的祝福詞狀態文字。

### 2026-07-21(攻略攻略秘笈與天數折抵文案白牌化動態置換)
- **問題**: 攻略（Guide）中的任務描述、認真/積極挑戰 bullets 與天數折抵清單（如「邀約入門體驗課」、「推薦報名初階課」）在大隊長設定與學員預覽時，仍會顯示原本的 NLP/入門體驗課等字樣，未與當前品牌動態替換。
- **改動範圍**:
  - `lib/brand.ts`:
    * 調整 `formatBrandText` 與 `formatAchievementText` 中的正規表達式，將 `邀約入門體驗課` 正確動態替換成 `邀約出來連結成功`，並將 `推薦報名初階課` 動態替換成 `傳愛成功`。
  - `components/Tabs/DailyQuestsTab.tsx`:
    * 渲染攻略秘笈列表（認真/積極要點 bullets, 天數折抵項目標題 `offset.title` 與描述 `offset.desc`）時，將字串包裹在 `formatBrandText(...)` 中進行動態替換。
  - `components/Admin/tabs/GuideTab.tsx`:
    * 在後台管理員攻略預覽面板中，同步將顯示內容包裹在 `formatBrandText(...)` 中進行動態替換。

### 2026-07-19(儀表板四力指標系統重構：連結力、行動力、影響力、傳愛力)
- **背景**: 使用者要求將個人儀表板「定課打卡」分頁底下的「四維指標」重構：
  - 更名四力指標：
    1. `感官敏銳度 (Sensory Acuity)` ➔ **`連結力 (Connection Power)`**
    2. `心錨穩定度 (Anchor Stability)` ➔ **`行動力 (Action Power)`**
    3. `親和感共鳴 (Rapport Resonance)` ➔ **`影響力 (Influence Power)`**
    4. `信念重塑力 (Belief Reshaping)` ➔ **`傳愛力 (Spread Love Power)`**
  - 動態綁定任務完成數（而非原先線性綁定總分數），設定加分比例如下（加分有感且不易太快達到 100%）：
    * **連結力**：完成並通過 **每日任務 (Daily)** ➔ 每次 +4% (25 次達 100%)。
    * **行動力**：完成並通過 **每週、特殊、限時任務** ➔ 每次 +8% (13 次達 100%)。
    * **影響力**：完成並通過 **邀約相關任務**（標題含「邀約」） ➔ 每次 +20% (5 次達 100%)。
    * **傳愛力**：完成並通過 **推薦或成交相關任務**（標題含「推薦」或「成交」） ➔ 每次 +25% (4 次達 100%)。
- **改動範圍**:
  - `components/Tabs/DailyQuestsTab.tsx`:
    * 重構 `attrAcuity`, `attrStability`, `attrRapport`, `attrReshaping` 的計算公式，改為從 `submissions` 與 `missions` 比對得出各分類已審核通過 (approved) 的任務筆數，並套用相應的百分比乘數。
    * 調整折疊按鈕與屬性列表的文字標籤為連結力、行動力、影響力、傳愛力與四力指標。

### 2026-07-13(生命靈數報告顯示規則限制 & 五運隱藏)
- **背景**: 使用者要求調整生命數字功能：
  - 將「五運・卡點與修煉功課」卡片區塊隱藏。
  - 在「查看完整文字解析」摺疊面板中，總共有五個大段落，調整為**只顯示第 1, 4, 5 段落**，且渲染序號需對齊顯示為「一、二、三」：
    * 顯示為 `一、核心位置解讀` (對應原始第 1 段)
    * 隱藏：二、缺少能量解析、三、重複/過多能量
    * 顯示為 `二、建議修煉功課` (對應原始第 4 段)
    * 顯示為 `三、最後開示總結` (對應原始第 5 段)
- **改動範圍**:
  - `components/Tabs/LifeNumberTab.tsx`: 
    * 使用 `false && (...)` 暫時隱藏整個「五運・卡點與修煉功課」卡片區塊。
    * 使用 `false && (...)` 隱藏折疊面板中的「二、缺少能量解析」與「三、重複/過多能量」段落。
    * 將剩餘段落的標題字串重新編排為「二、建議修煉功課」與「三、最後開示總結」，對齊視覺上的 1, 2, 3 順序。

### 2026-07-13(靈數命盤優化: 生命靈數九宮格中間數顯示三角形與加權計算)
- **問題**: 能量九宮格原本只有生日次數（圈）和後天數（三角）與主命數（方框）。使用者要求「中間數」也要在九宮格上顯示三角形。
- **改動範圍**:
  - `lib/numerologyCalc.ts`: 修改 `deriveFromBirthday` 函式。將收斂鏈（`chain`）中的「中間數」拆成個位數字，併入 `triangleDigits` 之中，以在能量加權（×2）與九宮格顯示時包含中間數數字。同步更新 `weightRule` 說明為 `圈（生日）×1、三角（後天/中間數）×2、正方形（主命數）×3`。
  - `components/Tabs/LifeNumberTab.tsx`: 將 UI 九宮格下方的圖例說明調整為 `三角 ＝ 後天/中間數`。

### 2026-07-13(Bugfix: 經驗明細/歷史紀錄頁面渲染錯誤修正)
- **問題**: 點擊歷史紀錄（HistoryTab）時，瀏覽器拋出 `Runtime TypeError: null is not an object (evaluating 'reason.toLowerCase')`。原因在於資料庫中的部分 `score_logs` 紀錄可能沒有原因 (`reason` 欄位為 `null`），造成在 `getLogIcon` 嘗試呼叫 `reason.toLowerCase()` 時出錯崩潰。
- **改動範圍**:
  - `components/Tabs/HistoryTab.tsx`: 將 `getLogIcon` 的參數改為 null-safe 寫法，當 `reason` 為空、`null` 或 `undefined` 時，直接回傳預設圖標，避免拋出 runtime 異常。

### 2026-07-13(雙品牌動態字串置換: UI 動態將所有「NLP」與「入門課」置換為當前品牌，並客製成就與邀約文字)
- **背景**: 為了讓切換成 `NEXT_PUBLIC_BRAND=ascension` 時，原本來自資料庫包含 "NLP"、"入門課"、"初階課" 的舊資料能在前端畫面（包括前/後台）中合適地呈現，且符合學員不混淆的原則：
  - 一般任務與課程中：將 "NLP" 換為 "揚升"，將 "初階課/初階" 換為 "能量學"（如排行榜的「推薦報名能量學」）。
  - 邀約與推薦動作轉換：
    * 任何「成功推薦 X 人報名 NLP 初階課」一律置換成「**成功傳愛 X 人報名能量學**」（例如推薦成就內容變更為 `成功傳愛 5 人報名能量學，成為同修生命中的貴人。`）。
    * 任何「參加/參與」的「NLP 入門體驗課/入門課」一律置換成「**出來連結成功**」；「邀約 NLP 入門課/體驗課」一律置換成「**邀約出來連結成功**」。
  - 所有的成就（Achievements）及修行稱號：**全面移除「揚升」二字**（直接將 "NLP" 拿掉改為空字串，例如「NLP 宗師」變為「宗師」）；將「推廣先鋒」改為「傳愛先鋒」。
- **改動範圍**:
  - `lib/brand.ts`: 新增 `formatBrandText`（用於任務與課程）與 `formatAchievementText`（用於成就與稱號）輔助置換函式。
  - `lib/titleLogic.ts`: 在 `getRankTitle` 與 `getNextRankTitle` 稱號回傳中使用 `formatAchievementText`（將「NLP大魔導師」動態化為「大魔導師」而非「揚升大魔導師」）。
  - `components/Tabs/AchievementsTab.tsx`, `components/Admin/tabs/OthersTab.tsx` (成就區塊), `components/Tabs/quests/MilestoneModal.tsx`: 改為使用 `formatAchievementText` 進行防污染置換。
  - `components/Tabs/CourseTab.tsx`, `DailyQuestsTab.tsx`, `SpecialQuestsTab.tsx`, `WeeklyTopicTab.tsx`, `LeaderboardTab.tsx`: 頁面名稱與描述渲染時維持使用 `formatBrandText` 置換。
  - `components/Tabs/quests/ConfirmModal.tsx`, `ProofModal.tsx`: 各種簽到與里程碑彈窗的任務字串維持使用 `formatBrandText`。
- **備註**: localStorage key 及程式碼內部變數（如 `nlp_*`）一律不受影響，維持系統登入與防呆機制正常運作。

### 2026-07-06(第三批 效能快贏 — 純程式,無 SQL)
`npm run build` 通過。
- **P1 lucide 圖示瘦身**:原本 `app/page.tsx` 與 `components/BadgeIcon.tsx` 用 `import * as Icons from 'lucide-react'` → 把 1500+ 顆圖示全打進學員端 bundle。
  - `app/page.tsx` 那行是**死 import**(全無使用)→ 直接刪。
  - 新增 `lib/badgeIcons.ts`:只 named import picker 用到的 99 顆圖示(100 顆中 `Tree` 不存在於 lucide → 省略,fallback Award,與改版前逐字相同),匯出 `BADGE_ICON_MAP` 與 `PRESET_BADGE_ICONS`。
  - `BadgeIcon.tsx` 改查 `BADGE_ICON_MAP[iconKey] || Award`;`OthersTab.tsx` 的 picker 清單改 import 自 `lib/badgeIcons`(單一真相,避免日後加圖示忘了同步)。
- **P2 經驗明細 memo 化**:`app/page.tsx` score_logs 顯示原本每次 render 全量 filter+map,且 map 內對 submissions/tasks/missions 巢狀 `.find`(O(logs×資料量))。改包 `useMemo` + 先建 id→物件 Map 查表 O(1)。
  - ⚠️ 陷阱:此值原本在 early return 之後計算;useMemo 是 hook 必須放在所有 early return **之前**,否則違反 hooks 規則。故 memo 移到 `// --- Render logic ---` 上方,並在 memo 內用同一套邏輯(`viewAsUserId`/`profiles`/`currentUser`)就地算出目標學員,`panelUser` 仍在原處(給其他地方用)。

### 2026-07-06(第二批 安全 — 上 main 前必須完成的 4 項)
只動測試庫/staging,`npm run build` 通過。**上 main 前**:程式碼 push + 三支 SQL 在(NLP 正式庫 + 揚升庫)各跑一次 + Vercel Production 確認 REQUIRE_SESSION。
- **S1 REQUIRE_SESSION**:`NEXT_PUBLIC_REQUIRE_SESSION=true` 本機/預覽已設。⚠️ 需人工確認 Vercel Production 也有設(沒有=沒 session 的人不會被導回登入,RLS 形同虛設)。
- **S2 submissions 讀取收緊**:原 `p_subs_select` 是 `using(true)` → 全班心得原文/圖任何人可 F12 下載。改「視圖 + 收緊 RLS」(比照 profiles 手機那批):
  - 新增公開視圖 `submissions_public`(只含非隱私欄位,繞過 base RLS)→ 排行榜計數用;
  - base table select 收緊為 **自己 + 見證牆(approved 且分享/自由分享) + 隊長本隊 + admin**;
  - `services/queries.ts` 批次計數改讀 `submissions_public`(`subsPublicBatch`,merge 最底層),自己/見證的完整心得仍讀 base(RLS 放行)→ 邀約王者/影響力之神當期排名不會因收緊而少算。
  - SQL:`docs/schema_fixes_36_submissions_privacy.sql`。
- **S3 API 冒用防護**:`update-member-settings` 與 `my-enrollments` 原本只信任前端自報的 `requesterId/id`(任何人改 body 冒用別人)。改成前端帶 session JWT(`lib/authToken.ts` 的 `authHeaders`),伺服器驗 token 取可信 auth uid(`lib/serverAuth.ts`),比對該 profile 的 `auth_user_id` 是否就是本人,不符回 401/403。呼叫端:`CaptainDashboard.handleSaveSettings`、`useTables` my-enrollments fetch。
- **S4 註冊只能 student**:`hooks/useAuth.ts` insert 由 `role: regData.role` 改寫死 `role: 'student'`(自封 captain=權限提升);RLS `p_profiles_insert` 同步收緊為只允許 student。SQL:`docs/schema_fixes_35_register_role_student.sql`。
- **待跑 SQL(NLP 正式庫 + 揚升庫 各一次)**:`schema_fixes_35`(註冊 role)、`schema_fixes_36`(submissions 視圖+RLS)。測試庫也要跑(這批 SQL 測試庫尚未跑)。

### 2026-07-06(第一批 P0 正確性 Bug 修復 — 全盤健檢後 8 項)
全部只動測試庫/staging,尚未 push、尚未問要不要上 main。`npm run build` 通過。
1. **通用任務學員看不到**:`app/page.tsx` 的 `filteredTasks` 與 `CaptainDashboard` 的 `batchTasks` 原本 `t.batch_id === panelBatchId`,把 `batch_id=null`(全部期數通用)任務濾掉了 → 改成 `!t.batch_id || t.batch_id === …`。(commit 7082035 起失效,補打卡/通用限時任務都受影響)
2. **PostgREST 只回最舊 1000 筆**:`services/queries.ts` fetchScoped 的 `score_logs / user_achievements / course_attendance / student_notes` 沒 `.order()/.limit()` → 補 `.order('created_at',{ascending:false}).limit(5000)`。
3. **防重複加分**:新增 `docs/schema_fixes_33_dedup_submissions.sql` —— 單次完成任務(max_completions=1)加 partial unique index 擋跨裝置/跨分頁重複打卡(前端鎖只擋單 session)。⚠️ 兩庫各跑、先跑步驟0查既有重複。
4. **隊長指揮所格子**:`hooks/useGameActions.ts` handleToggleCell 沒連點鎖、三個寫入沒檢查 error → 比照 checkInLock 加鎖 + 每個寫入檢查 error + 失敗 toast。
5. **排行榜同分名次亂跳**:`LeaderboardTab.tsx` 5 處排序加 tie-break(created_at 先達到者在前,再 id),不再每次 render 隨機互換。
6. **公告已讀共用裝置互吃**:`DailyQuestsTab.tsx` `nlp_read_announcements` 全域 key → 加 `_${profile.id}` 後綴(nlp_ 前綴是內部代號不改,只加 user 後綴)。
7. **多期學員登進舊期**:`app/api/auth/login/route.ts` profiles 查詢原 `limit=1` 無 order → 改 `order=created_at.desc` 且回傳前優先挑 `status=active` 那筆。
8. **攻略只存編輯者瀏覽器**:`lib/guideConfig.ts` 攻略清單從 localStorage 搬進 DB(`app_config` 表,key=`guide_list`)。同步 getter 讀記憶體快取,App 啟動 `loadGuidesFromDB()` 補水;後台 `saveGuidesToDB()` 寫回(檢查 error)。SQL:`docs/schema_fixes_34_app_config_guides.sql`(公開讀+is_admin() 才能寫),⚠️ 兩庫各跑。localStorage 降為離線鏡像。
- **待跑 SQL(兩個 Supabase 各一次)**:`schema_fixes_33`(防重複)、`schema_fixes_34`(app_config)。
>>>>>>> origin/staging

### 2026-07-06(品牌白牌化:一份程式碼服務 NLP + 揚升體系兩個品牌)
- **背景**:要開第二個品牌「揚升體系(傳愛連結力)」,但不想維護兩份程式碼 → 白牌化:所有品牌顯示文字集中到 **`lib/brand.ts`**,由環境變數 `NEXT_PUBLIC_BRAND` 切換(未設=NLP 預設,`ascension`=揚升體系)。**NLP 那組字串與改版前逐字相同**,既有正式站部署後不會有任何變化。
- 改動範圍:layout(標題/SEO)、page 頁尾、LoginForm 大標、神獸彈窗(SuccessModal/LevelUpModal)、DailyQuestsTab 修行文案、OthersTab 課程模板、PetsTab/BatchesTab/OthersTab placeholder、Captain/Reviews 接龍標題、guideConfig 複訓任務名、useAdminPeople 新班次自動命名前綴。
- **顯示相容雙品牌**:Header 前綴清單、LeaderboardTab `stripNLP`、TeamsTab 正則同時吃「NLP/揚升」前綴 —— 不跟品牌走,因為 DB 舊班次名可能兩種都有。
- ⚠️ **陷阱**:內部代號(localStorage `nlp_*` key、`@nlp.local` 登入信箱網域)**絕不可品牌化**,否則切品牌=全員登出/無法登入。brand.ts 檔頭有註記。
- 部署:兩個 Vercel 專案指同一 repo;NLP 站不設 BRAND 變數,揚升站設 `NEXT_PUBLIC_BRAND=ascension` + 自己的 Supabase 變數組。本機 `.env.local` 已設 ascension(預覽揚升)。

### 2026-07-04(補打卡改「手動」重新設計,取代自動護盾)
- 規則:每人每期 **5 次**補打卡;學員在護盾彈窗看到「最近缺的一天」→ 點「立即補打卡」提交**補打卡任務** →(依任務審核設定)審核通過 → **補回最近缺的那一天**(接得上連勝的缺口)+ 次數 −1。只補單天。
- SQL `docs/schema_fixes_30_makeup_redesign.sql`:streak_shields 預設改 5+重設;tasks 加 `is_makeup`;改寫 `_grant_shield_on_approve` → 只處理 is_makeup 任務、審核通過補回「最近缺的一天」(candidates 從 today-30~today-1 找最大的「D 缺且 D-1 有」)。
- 客戶端:TasksTab 加「🩹 設為補打卡任務」勾選(勾了自動 max_completions=0 可重複);DailyQuestsTab 隱藏 is_makeup 任務於一般任務區、算 recentMissingDay/canMakeup、火焰旁標籤改「🩹 補打卡 N 次」(有可補時紅色 pulse+紅點)、彈窗改補打卡流程(有缺→立即補打卡開該任務提交;無缺→說明);getTaskProgress 對 is_makeup 視為無限次。
- 舊的 reward_shields(完成任務得護盾)/自動用護盾 → 停用(欄位留著不影響)。

### 2026-07-03(連勝護盾 — 加入「賺取」機制 + 版面調整)
- **規則改為**:上限 3 張、學員從 0 開始(不再預設送),完成「有設獎勵護盾」的任務才獲得。
- **SQL 改用 `docs/schema_fixes_28_streak_shield_full.sql`(完整、可重複執行,取代 27)**:streak_shields 預設改 0+歸零、tasks/missions 加 `reward_shields`、獨立 trigger `_grant_shield_on_approve`(提交 approved 時發護盾、上限3、不倒扣、不動既有計分 trigger)。
- **後台**:TasksTab 任務新增「🛡️ 完成獎勵護盾 N 張」欄位(create+edit,寫入 tasks.reward_shields)。
- **學員端版面**:神獸介紹(stageDesc)移到成長等級下方(進化提示留在進化按鈕旁);護盾改成里程碑下方的面板(顯示 N/3 + 說明最多補3次 + 「前往任務賺護盾」按鈕跳限時分頁);有獎勵護盾的任務卡顯示「🛡️ 護盾 xN」標記。

### 2026-07-03(新功能:連勝護盾/補打卡卡 — 基礎版)
- 每位學員每期預設 2 張護盾;「今天有打卡、昨天漏、前天有」時自動用 1 張補昨天,連勝不斷(只補單天)。
- SQL `docs/schema_fixes_27_streak_shield.sql`:profiles 加 `streak_shields`、新表 `streak_shield_days`、函式 `use_streak_shield_if_needed`、`claim_streak_bonus` union 護盾日、`v_public_profiles` 補 streak_shields 欄。(測試+正式都要跑)
- 客戶端:`DailyQuestsTab` 載入本人護盾補日→算進連勝 done 集合、火焰旁顯示「🛡️ 剩 N 張」;`useGameActions` 每日打卡先呼叫 use_streak_shield_if_needed(用了跳提示)再 claim_streak_bonus。護盾補日用 localized query 載入(未走 fetchScoped 管線,blast radius 小)。
- **待做(階段2,使用者要求)**:任務可設「完成獎勵 🛡️ 護盾 N 張」,讓護盾可由完成任務賺取。

### 2026-07-03(資安二輪:連勝 race 修補 + check-exists 限流)
- **連勝加分 race**:`claim_streak_bonus` 的「查有沒有發過→沒有才發」非原子,快速連點兩個每日任務可重複領里程碑獎勵。修法:函式開頭加 `pg_advisory_xact_lock(hashtext(p_student_id))` 序列化同一學員的並發呼叫。SQL 見 `docs/schema_fixes_26_streak_race_fix.sql`(測試+正式都要跑)。⚠️ 只防未來,不自動退回過去可能的重複。
- **check-exists 限流**:`app/api/auth/check-exists/route.ts` 原本無限流,可被拿來暴力驗證「姓名+電話」組合。加了每 IP 記憶體限流(每分鐘>20 擋、fail-open、不進 DB 不污染 login 計數)。
- 設計面備註(未改):登入僅需姓名+電話=等於密碼;對 50 人內部班可接受,對外/變大再加 OTP/密碼。

### 2026-07-03(資安:proof_link 的 javascript: XSS 修補)
- **修**:學員提交的 `proof_link` / `proof_image_url` 原本直接當 `<a href>`,可注入 `javascript:`/`data:text/html`,審核者(含大隊長)點連結會在自己身分下執行(儲存型 XSS)。
- 新增 `lib/helpers.ts` 的 `safeLinkHref()`(只放行 http/https,無協議像網址就補 https://,其餘回 '#')、`safeImageHref()`(額外放行 data:image/)。
- 套用點:`WeeklyTopicTab.tsx`、`DailyQuestsTab.tsx`、`ReviewsTab.tsx`(proof_link ×3 + 佐證圖 href ×1)。
- 來源:F5 規格的資深健檢(唯一列為「該優先修」的一條);其餘為低風險/未來項。已驗證 RLS/計分/金鑰/race 都扎實。

### 2026-07-02(三項優化:學員載入瘦身②/監控每日摘要③/page.tsx 抽離④)
- **② 純學員載入瘦身**(`services/queries.ts` fetchScoped):
  - 純學員(role=student 且無 squad_role)不再載「全期他人的完整提交」→ 改載「自己的完整提交(SUB_COLS)」+「全期精簡欄位 SUB_COLS_SLIM(id,mission_id,task_id,student_id,status,created_at;無心得文字)」+ 見證牆完整(原有)。
  - **隊長 / 盯盯隊長(有 squad_role)維持全期完整**(keepBatchSubs=true;要審核、看隊員進度、接龍)。
  - 邊界已逐一驗證:排行榜邀約/影響力計數只需 slim 三欄✓;見證牆走獨立完整查詢✓;打卡/重複檢查/成就只用自己的✓。合併順序:slim→自己→見證(後蓋前)。
  - ⚠️ 若未來加「學員要看別人提交內容」的功能,記得 slim 模式沒有他人的 proof_text。
- **③ 系統健康分頁加每日摘要**(HealthTab):三張卡顯示「今日 · 昨日 N」+ 新增「近 7 日異常趨勢」長條圖。
- **④ page.tsx 瘦身**(1373→1282 行):25 個資料表 useState + applyTables 純搬移到 `hooks/useTables.ts`(名稱不變、行為不變)。applyTables 原閉包引用的 loadedProfile/currentUser/setUserEnrollments 改由呼叫端以 extra 參數傳入(求值時機相同)。
- 決策:任務截止「隔日中午12點」規則**維持不變**(使用者指示)。

### 2026-06-30(學員端:單次任務(tasks)與期數任務(missions)『並存顯示』)
- **重要修正**:以前學員端 `DailyQuestsTab` 用「有期數任務(missions)就不顯示任務管理(tasks)」的三元式 → 在「任務管理」建的**單次/臨時任務,有期數的學員根本看不到**(被 missions 擋掉,尤其學員做過舊任務時)。
- 改成**兩者並存**:同一分頁同時列出 `filteredMissions`(期數任務)+ `sortedTasks`(單次任務)。
- `filteredTasks` 加上**期數範圍過濾**:`t.batch_id` 有值就只給該期學員;沒值(全體/大會任務)給所有人 → 避免某期單次任務外洩給別期。
- `categoryHasUndone`(分頁小紅點)同步改為兩邊都看。
- 用途分工:**期數任務**=整期排程產生(期數任務設定);**任務管理(tasks)**=單次/臨時發放,現在學員看得到了。
- 註:task.type 只有 daily/weekly/temporary(對應特殊)/limited 會對到分頁;舊的 type='special' 不會顯示(等同停用)。

### 2026-06-30(任務可編輯 + 限時任務「指定日期」模式)
- **背景**:49 期限時任務被排成 6/20~6/23(早就過期)學員看不到。根因是限時任務日期一律「從開訓日推算」,且任務建立後無法編輯,只能下 SQL 救。
- **① 限時任務「指定日期」模式**:`期數任務設定`(BatchRulesTab)限時任務新增模式切換——「開訓第幾天」(原本)或「指定日期」(直接填發布/截止日)。
  - schema:`batch_mission_templates` 加 `abs_publish_date` / `abs_deadline_date`(見 `docs/schema_fixes_25_limited_abs_date.sql`,測試+正式都要跑)。
  - 兩欄都有值 → 產生/預覽改用絕對日期(useMissionGen + SchedulePreviewTab 都改了);截止日整天到 23:59。
  - ⚠️ 防呆:**只有真的選「指定日期」才會送 abs_* 欄位**,沒選就不送 → 即使 DB 還沒跑 SQL,既有套用功能也不會壞。
- **② 任務可編輯**(以前只能刪除,要改只能下 SQL):
  - `任務排程預覽`「已產生的任務」每筆加「編輯」→ 可改 標題/分數/發布日/截止日/狀態(`onUpdateMission` → missions update)。
  - `任務管理` 大會修行任務每筆加「編輯」(`onUpdateTask` → tasks update)。
- 日期寫入慣例:存 `YYYY-MM-DD 00:00:00+00`(發布)/`23:59:59+00`(截止),配合 parseTaipei「把存的牆上時鐘當台灣時間」。
- 註:e2e `core-flow` 在本機 sandbox 會因 dev server/測試庫載入慢而卡在登入欄逾時(clean code 也一樣),非本次改動造成;build + tsc 皆過。

### 2026-06-29(簡易監控:系統健康分頁)
- **新增主動監控**,不用再等學員回報才知道出事。三種事件由前端 fire-and-forget 寫進新表 `client_logs`:
  - `upload_fail`(圖片上傳失敗)、`submit_fail`(提交失敗)、`slow_load`(載入 >8 秒)。
- 後台新增分頁「**系統健康**」(`components/Admin/tabs/HealthTab.tsx`):今日摘要卡 + 最近 200 筆明細(誰/何時/原因)。
- 新檔:`lib/clientLog.ts`(logEvent helper,永不擋使用者、表不存在也靜默)、`docs/schema_fixes_24_client_logs.sql`(建表+RLS:任何人可寫、僅 is_admin 可讀)。
- 埋點:`useGameActions` handleCheckIn(上傳/提交失敗)、`app/page.tsx` fetchData(載入計時)。
- ⚠️ **要先在測試庫 + 正式庫各跑一次 schema_fixes_24 的 SQL** 才會開始收集;沒跑也不會壞(靜默 + 分頁顯示提示)。

### 2026-06-28(學員載入 10MB→0.6MB + 後台寫入全面補 error 檢查)
- **🔴 修『學員轉很久才進入』**:`fetchScoped` 提交用 `select('*')` 把 11 筆 base64 肥圖整包撈下 → 學員一登入下載 ~10MB。改成精簡欄位 + 圖片另撈(排除 base64),**10MB→0.6MB(省94%)**。(之前只修了 admin 的 fetchFull,學員漏掉。)
- **後台寫入全面補 error 檢查**:useAdminMisc/useAdminPeople/useMissionGen 約 15+ 個寫入原本沒檢查 `{error}`(假成功風險)。一律最小改動:`const { error } = await ...; if (error) { alert + return }`,成功路徑不動。
- 那 11 筆 base64 圖維持不顯示(使用者決定先不搬 Storage);效能已不受影響。

### 2026-06-27(以終為始每日卡片加日期與點擊防呆提示)
- 修復學員誤認提示框為提交按鈕的疑惑：將 `🎯 引導他人說出這個詞彙...` 加上 onClick 與 cursor 樣式，點擊時跳出 Toast 說明提示（此為日常修行提示，無須額外點擊提交）。
- 卡片正面加入 `修行日期：{drawnDate}` 標籤，便於學員對齊每日修行進度。

### 2026-06-27(補齊『Supabase 寫入未檢查 error』的假成功隱患)
- 通則:`insert/update/delete` 失敗回 `{error}` 不 throw → 沒檢查就會「假成功」。補上 error 檢查 + 提示/中止:
  - **handleReviewSubmission**(審核):update 失敗 → showToast + return(不做樂觀加分)。
  - **handleRegisterCourse / handleSaveNote**:失敗 → showToast + return。
  - **handleDeleteBatch**(刪期數):5 步改逐步檢查,任一失敗就**拋錯中止**(避免半毀);BatchesTab 呼叫端 try/catch + alert。
  - **handleGenerateMissions**(產生任務):catch 改 `throw`;SchedulePreviewTab try/catch,失敗顯示錯誤而非用部分結果跳成功。
- 教訓已收進 Part 1「Supabase 寫入一律檢查 error」。

### 2026-06-26(根治『指派職責 0 筆失敗』— 已上正式區)
- 根因:前端用 RLS 直接 update `profiles.squad_role` 靠 `is_captain_of(auth.uid())`;同一人跨期多帳號、無痕/過期 session 時 auth.uid 對不上 → 更新 0 筆靜默失敗(小隊長指派盯盯隊長被擋)。
- 修:新增 `app/api/captain/update-member-settings`(service role 執行,**伺服器端驗證 requester 是大隊長或該成員小隊的小隊長**才放行);CaptainDashboard 指派改呼叫此 API + onRefresh。本機實測:隊長可指派、非隊長 403。
- 教訓:**靠 `is_captain_of(auth.uid)` 的前端寫入容易因跨期帳號/session 失敗;敏感或會被擋的寫入改走伺服器 API(service role + 自驗權限)較穩。**

### 2026-06-25(歷屆邀約/影響力榜跨期統計 — 已上正式區;正式庫需跑 schema_fixes_23)
- 歷屆「邀約王者/影響力之神」原本用前端 `submissions`+`missions` 算,但學員只載當期 → 歷屆算不出別期。
- 改用 RPC `invite_influence_counts()`(`docs/schema_fixes_23`)跨所有期數統計;前端歷屆榜改用它,當期維持前端統計。RPC 未建立時靜默退回前端統計(不會壞)。
- **正式庫/測試庫都要跑 `schema_fixes_23`** 才會生效。

### 2026-06-25(🔴 修嚴重 bug:提交破1000筆後待審消失 — 已上正式區)
- 根因:`submissions` 達 1025 筆,後台撈提交沒 order/limit → PostgREST 預設只回最舊1000筆 → 最新待審被截掉 → 後台沒任務可審。
- 修:fetchFull 提交查詢改 `order created_at desc + limit 3000`,並**單獨撈所有 pending 合併去重**(絕不漏);fetchScoped 本期提交也加 desc+limit。詳見 Part 1「PostgREST 1000 筆上限」。

### 2026-06-25(明細加「任務審核狀態」— 已上正式區)
- 學員反映「不知道送出的任務過了沒/被退回沒」。在「明細」分頁頂端加切換:`經驗明細`(原有)｜`任務審核`(新)。
- 任務審核:列出學員自己所有提交,狀態分類(🟢已通過/🟡待審核/🔴已退回)+ 狀態篩選 chips(含筆數);退回會提示「可重新上傳」。全部提交都列(含每日自動通過)。
- `HistoryTab` 多收 `submissions`(學員自己的,page.tsx 的 `filteredSubmissions`)/`tasks`/`missions`;純前端、零資料庫變更。

### 2026-06-25(盯盯隊長 — 已上正式區;正式庫需跑 schema_fixes_22)
- 測試區驗收通過(觀察員可見指揮所、只顯示成員清單+接龍)後上正式(main)。
- **正式庫要跑 `docs/schema_fixes_22`**(加 `can_view_squad` 欄位 + 啟用既有「盯盯隊長」角色)才會生效。
- 過程修掉的相關 bug:① 指派職責原本完全沒提示 → 加成功/失敗提示;② 狀態被背景刷新蓋掉 → 改成只補缺鍵保留選擇;③ **RLS 擋更新時回 0 筆卻不報錯(假成功)→ 改用 `.select()` 檢查筆數、0 筆就誠實報錯**(`handleUpdateProfile`)。

### 2026-06-25(盯盯隊長 — 細節)
- **新增「盯盯隊長」角色**:被指派此小組角色的「學員」可**唯讀**檢視自己小組指揮所,只看「小隊成員清單 + 今日任務接龍」,其餘(審核/補簽/招募/矩陣/備註/本週之星)全藏;「檢視帳號」保留但唯讀。
  - 機制:`squad_roles` 加 `can_view_squad`(`docs/schema_fixes_22`);該旗標 true 的角色 = 盯盯隊長。
  - `app/page.tsx`:`isSquadObserver`(學員 + 其 squad_role 定義 can_view_squad)→ 開放「指揮所」分頁(Navigation `canViewSquad`)、帶出自己小組、傳 `observerMode`。
  - `CaptainDashboard`:`observerMode` 用兩個 `{!observerMode && (<>…</>)}` 片段把非必要區塊包起來隱藏。
  - 資安:角色仍是「學員」,RLS(`is_captain_of`)本就擋掉審核/調分/代打卡,純唯讀無漏洞。
  - 指派權:大隊長 + 小隊長皆可(沿用現有指派小組角色)。

### 2026-06-25
- **小隊長後台「🙈 見證牆顯示/隱藏」按鈕改為只給大隊長**(`CaptainDashboard.tsx` 約 1748 行,加 `currentUserRole === 'admin'` 包住)。原本小隊長看得到、但它只寫本機 localStorage(假隱藏)會誤導;真正上/下架見證由大隊長在「見證牆」分頁處理(寫 DB)。
- **TeamsTab 大隊長指派下拉**:`value={p.profile_id}` → `value={p.id}`(對齊 `director_id` 用 id 比對)。純預防,現有資料 profile_id 皆等於 id,行為不變、免搬資料。隊長候選下拉維持 `c.profile_id`。
- **score_logs 延後載入**:大隊長登入不載,開 `history`/`captain` 分頁才載一次。登入關鍵路徑底線降到 ~submissions 的時間。
- **圖片查詢並行化**:`fetchFull` 把圖片查詢併進 `Promise.all`(原本是撈完才另外 await,白白多等約 0.5~0.8s)。
- **ReviewsTab「已通過」清單**:改用 props 用 id 反查 學員/任務/經驗/審核者(不依賴 join);任務名加查 `missions.title`;經驗顯示真實 `score_awarded`;新增「審核者」(免審核任務顯示「系統自動通過」)。
- **review_type 自動同步**:改範本審核權限時,連帶更新既有 `missions.review_type`(`useAdminMisc.ts`)。附 `docs/schema_fixes_21` 對齊歷史資料。

### 2026-06(稍早)
- **uploadProofImage 不退回 base64**:上傳失敗改回 null(重試一次),根治「圖片把 DB 撐肥」。
- **fetchFull 不在 bulk 撈 proof_image_url**:改為另撈、排除 base64;`score_logs` 限流 1200(後改為延後載入)。
- **個資批①**:`v_public_profiles` 視圖 + 收緊 profiles RLS(`docs/schema_fixes_20`)。
- **joinData 改 Map 索引**:O(N×M) → O(N+M)。
